services:
  client:
    build: ./client
    ports:
      - "3001:3000"
    volumes:
      - ./client:/app
      - /app/node_modules
    depends_on:
      - server
    env_file:
      - ./client/.env
  
  server:
    build: ./server
    ports:
      - "5001:5000"
    environment:
      - MONGO_URI=mongodb://mongo:27017/ecommerce
    volumes:
      - ./server:/app
      - /app/node_modules
    env_file:
      - ./server/.env

 